# Flask + Redis Multi-Container Example
This example shows how to run Flask with Redis using Docker Compose.

### Build & Run
```bash
docker compose up -d
```
Then open: [http://localhost:5000](http://localhost:5000)

Each page refresh increments the Redis counter!
