# Universal Dockerfiles Repo (Generated)

This repo contains starter apps and **Universal Dockerfile** templates for:
- Python (Flask)
- Node.js (Express)
- Go (net/http)
- Java (Spring Boot / Maven)

It also includes a sample GitHub Actions CI workflow and a top-level README per app.

Build instructions:
- cd into any app folder, build the Docker image, e.g. `docker build -t python-app .`
- Run: `docker run -p 8000:8000 python-app`
